# LogiOptions (Flutter app)

macOS UI for LogiOptions. Run from repo root:

```bash
# from repo root
./scripts/mac-debug.sh
open dist/LogiOptions.app
```

Or:

```bash
flutter run -d macos
# (daemon must be running separately, or rebuild via mac-debug.sh)
```

Package name: `logi_options`.
